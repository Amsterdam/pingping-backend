# Types
type Media {
  type: MediaType!
  value: String!
}

# Input
input LocactionInput {
  lat: Float
  lon: Float
}

# Dynamic type for an task answer
scalar RouteAnswer

# Scalar
scalar Date
scalar Choices

# enums
enum Locale {
  nl_NL
}

enum MediaType {
  YouTube
  Image
}

enum TaskStatus {
  Completed
  Dismissed
  PendingUser
}

enum TaskType {
  DateOfBirth
  YesOrNo
  MultipleChoices
  Other
}

enum AchievementStatus {
  AvailableToEarn
  Earned
}

enum NotificationStatus {
  Initial
  Declined
  Approved
}
